<Application xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="CSIA.App"
             xmlns:local="using:CSIA"
             RequestedThemeVariant="Dark">
             <!-- "Default" ThemeVariant follows system theme variant. "Dark" or "Light" are other available options. -->

    <Application.DataTemplates>
        <local:ViewLocator/>
    </Application.DataTemplates>
  
    <Application.Styles>
        <FluentTheme />
        <StyleInclude Source="avares://CSIA/Icons.axaml" />
        <Style Selector="ListBox">
            <Setter Property="Background" Value="{DynamicResource ListBoxBackgroundColor}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ListBoxBorderColor}"/>
        </Style>
        <Style Selector="ListBoxItem">
            <Setter Property="Foreground" Value="#FFFFFFFF"/>
        </Style>
        <Style Selector="Window">
            <Setter Property="Background" Value="#1E1E1E"/>  <!-- Dark Gray -->
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <!-- Customize Button Styles -->
        <Style Selector="Button:MessageBox.Avalonia.Controls.MsButton">
            <Setter Property="Background" Value="#007ACC"/>  <!-- Blue -->
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
    </Application.Styles>
             
    <Application.Resources>
        <SolidColorBrush x:Key="ListBoxBackgroundColor" Color="#35670019"/>
        <SolidColorBrush x:Key="ListBoxBorderColor" Color="#258D0022"/>
        <FontFamily x:Key="LexendFont">avares://CSIA/Assets/Fonts#Lexend</FontFamily>
        <!-- <PathIcon x:Key="MaximButtonIcon" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" /> -->
        <ControlTheme x:Key="MenuBtton" TargetType="Button">
            <Setter Property="Background" Value="#753A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="7,4"/> <!-- Add Padding to Button -->
    
            <Setter Property="Template">
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <!-- Outer Border (Expanded by 2px on both sides) -->
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}"> <!-- Apply Padding to Border -->
                            <ContentPresenter x:Name="PART_ContentPresenter"
                                              Content="{TemplateBinding Content}"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              HorizontalContentAlignment="Center"
                                              VerticalContentAlignment="Center"
                                              TextBlock.TextAlignment="Center"
                                              Margin="2"/> <!-- Add Margin to ContentPresenter -->
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter>

            <!-- PointerOver Style -->
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#1AFFFFFF"/>
                <Setter Property="Foreground" Value="#FFFFFFFF"/>
            </Style>
        </ControlTheme>

        <ControlTheme x:Key="CloseButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="#50410010"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#00410010"/>
            <Setter Property="BorderThickness" Value="16"/>
            <Setter Property="CornerRadius" Value="100"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Ellipse Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource pane_close_regular}" 
                                  Width="10"
                                  Height="10"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change size or color -->
                    </Template>
                </Setter.Value>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50C54E81"/>
                <Setter Property="Foreground" Value="#50410010"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource pane_close_regular}" 
                                      Width="10"
                                      Height="10"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change size or color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="MinimButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="#50410010"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#00410010"/>
            <Setter Property="BorderThickness" Value="16"/>
            <Setter Property="CornerRadius" Value="100"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Ellipse Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource arrow_minimize_regular}" 
                                  Width="10"
                                  Height="10"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change size or color -->
                    </Template>
                </Setter.Value>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50C54E81"/>
                <Setter Property="Foreground" Value="#50410010"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource arrow_minimize_regular}" 
                                      Width="10"
                                      Height="10"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change size or color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="MaximButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="#50410010"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#00410010"/>
            <Setter Property="BorderThickness" Value="16"/>
            <Setter Property="CornerRadius" Value="100"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Width" Value="16"/>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Ellipse Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource arrow_maximize_regular}" 
                                  Width="10"
                                  Height="10"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change size or color -->
                    </Template>
                </Setter.Value>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50C54E81"/>
                <Setter Property="Foreground" Value="#50410010"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource arrow_maximize_regular}" 
                                      Width="10"
                                      Height="10"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change size or color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="UploadButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="Black"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#50C54E81"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Width" Value="37"/>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource disk_upload}" 
                                  Width="14"
                                  Height="14"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                    </Template>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Rectangle Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 RadiusX="7"
                                 RadiusY="7"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50670019"/>
                <Setter Property="Foreground" Value="#50C54E81"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource disk_upload}" 
                                      Width="14" 
                                      Height="14"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="DownloadButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="Black"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#50C54E81"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Width" Value="37"/>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource disk_download}" 
                                  Width="14" 
                                  Height="14"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                    </Template>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Rectangle Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 RadiusX="7"
                                 RadiusY="7"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50670019"/>
                <Setter Property="Foreground" Value="#50C54E81"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource disk_download}" 
                                      Width="14" 
                                      Height="14"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="FolderAddButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="Black"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#50C54E81"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Width" Value="37"/>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource add_folder}" 
                                  Width="14" 
                                  Height="14"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                    </Template>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Rectangle Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 RadiusX="7"
                                 RadiusY="7"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50670019"/>
                <Setter Property="Foreground" Value="#50C54E81"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource add_folder}" 
                                      Width="14" 
                                      Height="14"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="DeleteButton" TargetType="Button">
            <!-- <Setter Property="Background" Value="Black"/> -->
            <Setter Property="Background" Value="#50410010"/>
            <Setter Property="Foreground" Value="#50C54E81"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Width" Value="37"/>
            <Setter Property="Content">
                <Setter.Value>
                    <Template>
                        <PathIcon Data="{StaticResource delete_item}" 
                                  Width="14" 
                                  Height="14"
                                  Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                    </Template>
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <ControlTemplate>
                    <Panel>
                        <Rectangle Fill="{TemplateBinding Background}"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Stretch"
                                 RadiusX="7"
                                 RadiusY="7"/>
                        <ContentPresenter x:Name="PART_ContentPresenter"
                                          Content="{TemplateBinding Content}"
                                          Margin="{TemplateBinding Padding}"/>
                    </Panel>
                </ControlTemplate>
            </Setter>
      
            <Style Selector="^:pointerover">
                <Setter Property="Background" Value="#50670019"/>
                <Setter Property="Foreground" Value="#50C54E81"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                
                <Setter Property="Content">
                    <Setter.Value>
                        <Template>
                            <PathIcon Data="{StaticResource delete_item}" 
                                      Width="14" 
                                      Height="14"
                                      Foreground="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}"/> <!-- Change color -->
                        </Template>
                    </Setter.Value>
                </Setter>
            </Style>
        </ControlTheme>
        <ControlTheme x:Key="ListBx2" TargetType="ListBox">
            <Setter Property="Background" Value="Aqua"/>
        </ControlTheme>
    </Application.Resources>
</Application>